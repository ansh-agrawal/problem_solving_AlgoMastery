"""
Task 85: Distinct Subsequences

Given two strings s and t, return the number of distinct subsequences of s which equals t.
A subsequence of a string is a new string formed from the original string by deleting some (can be none) characters
without changing the relative order of the remaining characters.

Example:
    distinct_subsequences("rabbbit", "rabbit") -> 3
    distinct_subsequences("babgbag", "bag") -> 5
    distinct_subsequences("abc", "abc") -> 1
"""

from typing import Any

def distinct_subsequences(s: str, t: str) -> int:
    """
    Count the number of distinct subsequences of s that equal t.

    Args:
        s (str): Source string
        t (str): Target string

    Returns:
        int: Number of distinct subsequences
    """
    # TODO: implement
    pass
