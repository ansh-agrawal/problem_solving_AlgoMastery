"""
Task 78: Insert Interval

Given a list of non-overlapping intervals sorted by their start time (0-indexed), and a new interval, insert the new interval into the intervals such that the intervals remain sorted and non-overlapping (merge if necessary).

Example 1:
    Input: intervals = [[1,3],[6,9]], new_interval = [2,5]
    Output: [[1,5],[6,9]]

Example 2:
    Input: intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], new_interval = [4,8]
    Output: [[1,2],[3,10],[12,16]]

Args:
    intervals (list[list[int]]): List of non-overlapping intervals (0-indexed)
    new_interval (list[int]): Interval to insert (0-indexed)

Returns:
    list[list[int]]: Updated list of intervals
"""

def insert_interval(intervals: list[list[int]], new_interval: list[int]) -> list[list[int]]:
    """
    Insert Interval.

    Args:
        intervals (list[list[int]]): List of non-overlapping intervals (0-indexed)
        new_interval (list[int]): Interval to insert (0-indexed)

    Returns:
        list[list[int]]: Updated list of intervals
    """
    # TODO: implement
    pass

