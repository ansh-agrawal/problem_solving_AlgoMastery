"""
Task 86: Partition Labels

A string s of lowercase letters is given. Partition the string into as many parts as possible
so that each letter appears in at most one part, and return a list of integers representing the sizes of these parts.

Example:
    partition_labels("ababcbacadefegdehijhklij") -> [9,7,8]
    partition_labels("eccbbbbdec") -> [10]
    partition_labels("abcd") -> [1,1,1,1]
"""

from typing import Any, List

def partition_labels(s: str) -> List[int]:
    """
    Partition a string into parts so that each letter appears in at most one part.

    Args:
        s (str): Input string

    Returns:
        List[int]: List of partition sizes
    """
    # TODO: implement
    pass
