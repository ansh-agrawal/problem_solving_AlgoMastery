"""
Task 98: Minimum Cuts Palindrome Partitioning

Given a string s, partition s such that every substring of the partition is a palindrome.
Return the minimum number of cuts needed for a palindrome partitioning.

Example:
    minimum_cuts_palindrome_partitioning("aab") -> 1  # "aa" | "b"
    minimum_cuts_palindrome_partitioning("a") -> 0    # already a palindrome
    minimum_cuts_palindrome_partitioning("ab") -> 1   # "a" | "b"
"""

from typing import Any

def minimum_cuts_palindrome_partitioning(s: str) -> int:
    """
    Compute the minimum number of cuts to partition a string into palindromes.

    Args:
        s (str): Input string

    Returns:
        int: Minimum number of cuts needed
    """
    # TODO: implement
    pass
