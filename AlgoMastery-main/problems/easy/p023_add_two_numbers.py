"""
Task 23: Add Two Numbers

Given two non-empty linked lists representing two non-negative integers in reverse order, add the two numbers and return the sum as a linked list.

Example:
    add_two_numbers(l1=[2,4,3], l2=[5,6,4]) -> [7,0,8]  # (342 + 465 = 807)

Args:
    l1 (list[int]): First number as a list (reverse order, 0-indexed)
    l2 (list[int]): Second number as a list (reverse order, 0-indexed)

Returns:
    list[int]: The sum as a list (reverse order)
"""

def add_two_numbers(l1: list[int], l2: list[int]) -> list[int]:
    """
    Add Two Numbers.

    Args:
        l1 (list[int]): First number as a list (reverse order, 0-indexed)
        l2 (list[int]): Second number as a list (reverse order, 0-indexed)

    Returns:
        list[int]: The sum as a list (reverse order)
    """
    pass
