"""
Task 22: Letter Combinations of a Phone Number

Given a string containing digits from 2-9 inclusive, return all possible letter combinations that the number could represent. Return the answer in any order.

Example:
    letter_combinations_of_a_phone_number(digits="23") -> ["ad","ae","af","bd","be","bf","cd","ce","cf"]

Args:
    digits (str): The input digit string (0-indexed)

Returns:
    list[str]: All possible letter combinations
"""

def letter_combinations_of_a_phone_number(digits: str) -> list[str]:
    """
    Letter Combinations of a Phone Number.

    Args:
        digits (str): The input digit string (0-indexed)

    Returns:
        list[str]: All possible letter combinations
    """
    pass
