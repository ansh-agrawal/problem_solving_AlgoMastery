"""
Task 24: Merge k Sorted Lists

Given an array of k linked lists, each list is sorted in ascending order, merge all the lists into one sorted linked list and return it as a list.

Example:
    merge_k_sorted_lists(lists=[[1,4,5],[1,3,4],[2,6]]) -> [1,1,2,3,4,4,5,6]

Args:
    lists (list[list[int]]): List of k sorted lists (0-indexed)

Returns:
    list[int]: Merged sorted list
"""

def merge_k_sorted_lists(lists: list[list[int]]) -> list[int]:
    """
    Merge k Sorted Lists.

    Args:
        lists (list[list[int]]): List of k sorted lists (0-indexed)

    Returns:
        list[int]: Merged sorted list
    """
    pass
