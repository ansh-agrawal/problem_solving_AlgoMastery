"""
Task 13: Next Permutation

Given nums, rearrange them into the lexicographically next greater permutation (0-indexed). If not possible, rearrange as the lowest possible order.

Example:
    next_permutation(nums=[1,2,3]) -> [1,3,2]
    next_permutation(nums=[3,2,1]) -> [1,2,3]

Args:
    nums (list[int]): List of integers (0-indexed)

Returns:
    list[int]: Next permutation
"""

def next_permutation(nums: list[int]) -> list[int]:
    """
    Next Permutation.

    Args:
        nums (list[int]): List of integers (0-indexed)

    Returns:
        list[int]: Next permutation
    """
    pass
