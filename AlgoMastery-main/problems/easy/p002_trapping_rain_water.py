"""
Task 2: Trapping Rain Water

Given an array height representing elevation map, compute how much water it can trap after raining.

Example:
    trapping_rain_water(height=[0,1,0,2,1,0,1,3,2,1,2,1]) -> 6
    trapping_rain_water(height=[4,2,0,3,2,5]) -> 9

Args:
    height (list[int]): Elevation map (0-indexed)

Returns:
    int: Total units of trapped water
"""

def trapping_rain_water(height: list[int]) -> int:
    """
    Trapping Rain Water.

    Args:
        height (list[int]): Elevation map (0-indexed)

    Returns:
        int: Total units of trapped water
    """
    # TODO: implement
    pass
