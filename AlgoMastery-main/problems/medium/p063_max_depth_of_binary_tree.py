"""
Task 63: Max Depth of Binary Tree

Given the root of a binary tree, return its maximum depth.
The maximum depth is the number of nodes along the longest path from the root down to the farthest leaf node.

Example:
    root = TreeNode(3)
    root.left = TreeNode(9)
    root.right = TreeNode(20)
    root.right.left = TreeNode(15)
    root.right.right = TreeNode(7)

    max_depth_of_binary_tree(root) -> 3
"""

from typing import Any

def max_depth_of_binary_tree(root: Any) -> int:
    """
    Compute the maximum depth of a binary tree.

    Args:
        root (Any): Root node of the binary tree

    Returns:
        int: Maximum depth of the tree
    """
    # TODO: implement
    pass
