"""
Task 47: Jump Game II

Given an array nums, return the minimum number of jumps to reach the last index (0-indexed). Each element in nums represents your maximum jump length at that position.

Example:
    jump_game_ii(nums=[2,3,1,1,4]) -> 2
    jump_game_ii(nums=[2,3,0,1,4]) -> 2

Args:
    nums (list[int]): List of integers (0-indexed)

Returns:
    int: Minimum number of jumps to reach the last index
"""

def jump_game_ii(nums: list[int]) -> int:
    """
    Jump Game II.

    Args:
        nums (list[int]): List of integers (0-indexed)

    Returns:
        int: Minimum number of jumps to reach the last index
    """
    # TODO: implement
    pass
