"""
Task 41: Coin Change

Given coins of different denominations and an amount, return the fewest number of coins needed to make up that amount. If that amount cannot be made up by any combination of the coins, return -1. Coins are 0-indexed.

Example:
    coin_change(coins=[1,2,5], amount=11) -> 3
    coin_change(coins=[2], amount=3) -> -1

Args:
    coins (list[int]): List of coin denominations (0-indexed)
    amount (int): Target amount

Returns:
    int: Fewest number of coins needed, or -1 if impossible
"""

def coin_change(coins: list[int], amount: int) -> int:
    """
    Coin Change.

    Args:
        coins (list[int]): List of coin denominations (0-indexed)
        amount (int): Target amount

    Returns:
        int: Fewest number of coins needed, or -1 if impossible
    """
    # TODO: implement
    pass
