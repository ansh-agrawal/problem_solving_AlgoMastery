"""
Task 46: Jump Game

Given an array nums, return True if you can reach the last index starting from the first index. Each element in nums represents your maximum jump length at that position (0-indexed).

Example:
    jump_game(nums=[2,3,1,1,4]) -> True
    jump_game(nums=[3,2,1,0,4]) -> False

Args:
    nums (list[int]): List of integers (0-indexed)

Returns:
    bool: True if you can reach the last index, False otherwise
"""

def jump_game(nums: list[int]) -> bool:
    """
    Jump Game.

    Args:
        nums (list[int]): List of integers (0-indexed)

    Returns:
        bool: True if you can reach the last index, False otherwise
    """
    # TODO: implement
    pass
