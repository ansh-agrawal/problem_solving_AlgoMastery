"""
Task 45: Edit Distance

Given two strings word1 and word2, return the minimum number of operations required to convert word1 to word2. Operations are insert, delete, or replace a character.

Example:
    edit_distance(word1="horse", word2="ros") -> 3
    edit_distance(word1="intention", word2="execution") -> 5

Args:
    word1 (str): First string (0-indexed)
    word2 (str): Second string (0-indexed)

Returns:
    int: Minimum number of operations
"""

def edit_distance(word1: str, word2: str) -> int:
    """
    Edit Distance.

    Args:
        word1 (str): First string (0-indexed)
        word2 (str): Second string (0-indexed)

    Returns:
        int: Minimum number of operations
    """
    # TODO: implement
    pass
