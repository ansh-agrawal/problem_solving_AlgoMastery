"""
Task 54: Subsets

Given an integer array nums (0-indexed), return all possible subsets (the power set). The solution set must not contain duplicate subsets.

Example 1:
    Input: nums = [1,2,3]
    Output: [[],[1],[2],[1,2],[3],[1,3],[2,3],[1,2,3]]

Example 2:
    Input: nums = [0]
    Output: [[],[0]]

Args:
    nums (list[int]): List of integers (0-indexed)

Returns:
    list[list[int]]: All possible subsets
"""

def subsets(nums: list[int]) -> list[list[int]]:
    """
    Subsets.

    Args:
        nums (list[int]): List of integers (0-indexed)

    Returns:
        list[list[int]]: All possible subsets
    """
    # TODO: implement
    pass

